{
  "name": "maintenance-notice-checker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "fetch": "node scripts/fetch.mjs",
    "build": "node scripts/build.mjs",
    "test": "node --test",
    "serve": "node scripts/serve.mjs",
    "verify": "node scripts/verify.mjs"
  },
  "dependencies": {
    "cheerio": "1.1.2"
  },
  "devDependencies": {
    "playwright": "1.62.1"
  }
}
